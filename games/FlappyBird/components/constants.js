import { Dimensions } from 'react-native';

const { width, height } = Dimensions.get("screen");

export const MAX_WIDTH = width;
export const MAX_HEIGHT = height;
export const PIPE_WIDTH = 60;
export const GAP_SIZE  = 220;
