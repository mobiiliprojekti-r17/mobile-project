import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  bird: {
    position: 'absolute',
    backgroundColor: 'yellow',
    width: 40,
    height: 30,
    left: 50,
    borderRadius: 10,
  },
});
